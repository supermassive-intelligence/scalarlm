# Minimal requirements for MLX inference (no training)
# This avoids mpi4py which requires MPI libraries

# Core dependencies
numpy>=2.0.0
opencv-python>=4.12.0.88
jsonlines
aiofiles
persist-queue
matplotlib
streaming-form-data==1.15.0
openai>=1.40.0
openai-harmony
humanize
atomicwrites
cryptography
sortedcontainers
datasets

# FastAPI for API server
fastapi
fastapi-utils
typing-inspect  # Required by fastapi-utils
uvicorn
pydantic

# MLX for Apple Silicon
mlx
mlx-lm

# Skip mpi4py - only needed for training
# mpi4py==4.0.3
