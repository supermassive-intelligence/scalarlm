torchao>=0.16.1
accelerate
# Keep the validated Transformers line. This layer uses --no-deps, so its
# tokenizer constraint must be explicit too: vLLM can install tokenizers
# 0.23.1 before this layer downgrades Transformers to 5.12.x, which requires
# tokenizers<=0.23.0 and otherwise fails at import time.
transformers>=5.5.0,<5.13
tokenizers>=0.22.0,<=0.23.0
huggingface_hub>=0.37
peft
# InternVL (and other hub VLMs) ship custom modeling code whose vision tower
# imports timm; without it AutoModel load raises "requires ... timm".
timm
